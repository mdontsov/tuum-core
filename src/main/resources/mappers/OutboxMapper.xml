<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ee.tuum.banking.messaging.OutboxMapper">
    <insert id="insert">
        INSERT INTO outbox_events
            (id, aggregate_type, aggregate_id, event_type, payload, occurred_at, published_at, attempts)
        VALUES
            (#{id}, #{aggregateType}, #{aggregateId}, #{eventType}, CAST(#{payload} AS JSONB),
             #{occurredAt}, #{publishedAt}, #{attempts})
    </insert>

    <select id="lockUnpublished" resultType="ee.tuum.banking.messaging.OutboxEvent">
        SELECT id, aggregate_type, aggregate_id, event_type, payload::text AS payload,
               occurred_at, published_at, attempts
        FROM outbox_events
        WHERE published_at IS NULL
        ORDER BY occurred_at, id
        FOR UPDATE SKIP LOCKED
        LIMIT #{limit}
    </select>

    <update id="markPublished">
        UPDATE outbox_events
        SET published_at = #{publishedAt}, attempts = attempts + 1
        WHERE id = #{id}
    </update>

    <select id="countUnpublished" resultType="long">
        SELECT COUNT(*) FROM outbox_events WHERE published_at IS NULL
    </select>
</mapper>
